# Verified working combination on CPU (Python 3.10).
# transformers must stay on 4.x: Florence-2's trust_remote_code modeling file
# is not compatible with the transformers 5.x API.
transformers==4.49.0
torch==2.8.0
Pillow==12.3.0
timm==1.0.29
einops==0.8.2
